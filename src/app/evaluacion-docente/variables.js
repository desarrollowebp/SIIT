const pregunta = '?' // String
const materia = {...rest, maestro: {_id: 'xyz'}, nombre: "mate"}
const respuesta = 'R'

const preguntas = [{ pregunta }]
const respuestas = [{materia, respuestas: [respuesta]}]

// Modifica alumno hizoEvaluacionDocente: true
